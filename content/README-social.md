# Social queue usage

`social-batch-01.csv` is the machine-readable creative queue.
`social-links-batch-01.csv` contains per-platform UTM destinations.
`../social-content-batch-01.md` contains the human-readable scripts and safeguards.

When platform publishing integrations become available, these files can be used as inputs without changing the product database.
