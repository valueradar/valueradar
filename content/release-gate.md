# Release gate

Merge only when the branch remains based on current main, product URLs are allowlisted, product pages/sitemap are present, price language is transparent, and automated catalogue validation is configured. After merge, verify the live site before beginning social promotion.
