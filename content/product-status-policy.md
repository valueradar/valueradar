# Product lifecycle

- `draft`: research exists but one or more publishing gates are incomplete.
- `published`: evidence is sufficient for a public ValueRadar page.
- `archived`: no longer suitable for active recommendation, for example because the listing disappeared or the product became materially outdated.

Archiving should remove a product from active generated discovery/sitemap on the next build while preserving research history in version control.
