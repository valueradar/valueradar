# Performance roadmap

Keep ValueRadar lightweight:
- static HTML/CSS/JS by default;
- optimize product images before publishing;
- avoid unnecessary third-party scripts;
- lazy-load below-the-fold images when real imagery is added;
- monitor Core Web Vitals as traffic grows;
- do not add a heavy framework unless it solves a measured problem.
