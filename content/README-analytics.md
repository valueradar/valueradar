# Analytics ownership

GA4 remains the private measurement layer; do not add a public visitor counter.

Use ValueRadar's own `marketplace_click` event for outbound commerce intent. Once affiliate programs are active, marketplace reports remain the source of truth for attributed orders/commission and should be reconciled against GA4 traffic rather than treated as identical datasets.
